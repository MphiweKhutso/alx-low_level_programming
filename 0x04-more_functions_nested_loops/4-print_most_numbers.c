#Include "main.h"

/**
 * print_most_numbers - print the numbers 0 to 9
 * without 2 ad 4
 * Return: Always 0
 */
void print_most_numbers(void)
{
	int = 0;

	while (a < 10)
	{
		if (a != 2 && a != 4)
			_putchar(a + '0');

		a++;
	}
	_putchar ('\n');
}
